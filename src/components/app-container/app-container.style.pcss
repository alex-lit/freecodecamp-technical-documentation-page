.app-container {
  display: grid;
  grid-template-columns: 320px auto;
}
.app-container__sidebar {
  background-color: var(--indigo);
  padding: 40px 20px;
  color: var(--white);
  height: 100vh;
}
.app-container__main {
  padding: 40px 20px;
  height: 100vh;
  overflow: auto;
}
